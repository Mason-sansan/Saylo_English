# 定标 / placement 改动前快照

- **时间**：2026-04-22  
- **内容**：`quickReport.js` 与 `server.js` 的完整副本（引入 `netCapForPlacementPrior`、WebSocket `client_state`、`report.placement` 之前）

恢复步骤见 `web/ROLLBACK-PLACEMENT.md`。
